swagger: "2.0"
info:
  version: "1.0.0"
  title: Invalid API

paths:
  /users:
    parameters:
      - name: people
        in: body
        schema:
          type: array
    post:
      responses:
        default:
          description:  hello world
