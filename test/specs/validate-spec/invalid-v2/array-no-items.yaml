swagger: "2.0"
info:
  version: "1.0.0"
  title: Invalid API

paths:
  /users:
    parameters:
      - name: tags
        in: query
        type: array
    get:
      responses:
        default:
          description:  hello world
