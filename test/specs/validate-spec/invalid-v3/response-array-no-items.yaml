openapi: "3.0.2"
info:
  version: "1.0.0"
  title: Invalid API

paths:
  /users:
    get:
      responses:
        default:
          description:  hello world
          content:
            application/json:
              schema:
                type: array
