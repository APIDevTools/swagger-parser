swagger: "3.0"
info:
  version: "1.0.0"
  title: newer version
  description: This swagger spec is invalid because version 3.0 is not supported
paths:
  /users:
    get:
      responses:
        "200":
          description: hello world
