swagger: "1.2"
info:
  version: "1.0.0"
  title: old version
  description: This swagger spec is invalid because version 1.2 is not supported
paths:
  /users:
    get:
      responses:
        "200":
          description: hello world
