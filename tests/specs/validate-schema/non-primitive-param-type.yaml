swagger: "2.0"
info:
  version: "1.0.0"
  title: Invalid API

paths:
  /users:
    parameters:
      - name: user
        in: query
        type: object      # <---- only primitive types are allowed for params
    post:
      responses:
        default:
          description:  hello world
